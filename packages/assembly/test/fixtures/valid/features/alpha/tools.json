[
  {
    "id": "alpha.do",
    "featureIds": ["alpha"],
    "description": "alpha 功能的示例操作",
    "params": { "type": "object" },
    "execution": "client",
    "riskTier": "auto",
    "adapter": { "method": "GET", "urlTemplate": "/api/alpha" },
    "resultSchema": { "type": "object" }
  },
  {
    "id": "beta.only",
    "featureIds": ["beta"],
    "description": "仅挂载于 beta 的工具（装配 alpha 时应被过滤）",
    "params": { "type": "object" },
    "execution": "client",
    "riskTier": "auto",
    "adapter": { "method": "GET", "urlTemplate": "/api/beta" },
    "resultSchema": { "type": "object" }
  }
]
