# M4 全量门 + 评测走查记录

- 日期：2026-07-04
- 范围：M4 审计+评测门——audit jsonl sink + server 全链路事件埋点 + eval runner（五维度≥3跑）+ 审计完整性
- 判定基线：`.claude/rules/ZA-EVAL.md`、`.claude/rules/ZA-WHERE.md` U6、`evals/README.md`、建设提示词 Goal-f

---

## 一、全量门实跑结果（全绿）

| 门 | 命令 | 结果 | 证据 |
|---|---|---|---|
| 构建 | `pnpm -r build` | ✅ 8 workspace 全 Done | — |
| 单测 | `pnpm -r --workspace-concurrency=1 test` | ✅ 195 测试全绿 | contracts 54 / assembly 20 / audit 5 / llm-port 21 / toolgate 21 / server 36 / extension 38 |
| 依赖 lint | `pnpm lint:deps` | ✅ exit 0 | audit 加 ajv devDep 后 U2 星形约束仍成立 |
| 评测 runner | `pnpm eval` | ✅ exit 0 | 13/13 场景 3/3；审计完整性 PASS（99 事件） |
| E2E 全回归 | `pnpm test:e2e{,:m2,:m3}` | ✅ | M1/M2/M3 六场景无退化（audit 埋点为 record-only 旁路，不改帧/流） |

---

## 二、评测 runner（`scripts/evals/run.mjs`，协议层驱动，≥3 跑）

runner 不用浏览器，用 HTTP+SSE 直接驱动 server+mock，扮演客户端（回 hitl-decision / exec-result）。
每场景跑 3 次、需 3/3 才算通过（ZA-C-EVAL-02）。五维度全绿（详见 `2026-07-04-eval.md`）：

| ZA-EVAL 维度 | 对应场景 dimension | 全绿 |
|---|---|---|
| 讲解正确 | explain(4) + assembly-swap(1) | ✅ 5/5 |
| 引导命中 | guide(2) | ✅ 2/2 |
| 工具触发 | tool(2) | ✅ 2/2 |
| HITL 触发 | hitl(2) | ✅ 2/2 |
| 拒答边界 | refusal(2) | ✅ 2/2 |

---

## 三、审计完整性（Goal-f）

- audit sink（`packages/audit`）：record-only 旁路 jsonl（追加、自动建目录、落盘前脱敏兜底、
  失败只本地告警不抛——旁路铁律 U6/C6），单测 5 项。
- server 五类事件埋点（gateway）：session-start / assembly / tool-decision / hitl-verdict / tool-execution，
  只记 id/结局/摘要，不含实参/响应体/签名/secret（脱敏前置由构造保证 + sink 再兜一层）。
- eval runner 跑完读 `.za/eval-events.jsonl`：99 事件全过 audit-event schema、五类事件链齐、无 secret 样式命中 → PASS。
- server 集成测试另验：完整 HITL 后事件链齐全且无 secret/签名值；forbidden 有 tool-decision(deny) 无 tool-execution；
  audit sink 不可写时会话主链路不受影响（旁路铁律集成级验证）。

---

## 四、本期 assets/mock 改动的 ZA-EVAL 走查

- `scripts/mock-llm/server.mjs` 新增 R4 规则：问句含"为什么"+"待发货/状态" → 据 feature.md ZA-FEAT-01
  产出"业务原因不在配置内、需联系订单管理员"文本（不编造）。此前 m1-explain-03 因 mock 无该确定性规则
  返回 MOCK-DEFAULT、评测 0/3；补规则后 3/3。这是完善确定性测试替身以覆盖"讲解正确之不编造"维度，
  非把答案写进 feature.md（不违 EVAL-03——feature.md/facts.md 未改）。
- 改动后跑 llm-port 测试（mock 消费方）回归 21 绿；eval 全量 13/13；E2E 无退化。
- 真实 LLM 讲解质量评测锚点：接真实 provider 后以本 runner 换 mock 端点复跑（评测框架已就绪）。
