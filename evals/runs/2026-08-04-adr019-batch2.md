# adr-019 批次②③（声明式 preparation + automations 迁移）评测记录 — 2026-08-04

改动面：xianyu-seller pack 0.7.0→0.8.0（两个 execute-intent 工具补 `authorization.preparation`；
feature.md / SKILL.md 中 prepare 工具名改派生名 `prepare.<toolId>`）；服务端删除 xianyu 连接器、
改由通用引擎（`apps/server/src/prepare-intent.ts`）按声明派生；根快照 1.1.0→1.2.0。

证据环境：Node `>=22`；`pnpm@10.32.1`；LLM=确定性 mock（非真实模型）。
runner：`scripts/evals/run.mjs`（全量：host-demo 根 + acceptance 根 + assets 生产快照根）；
每场景重复 3 次，3/3 全过才算通过（ZA-C-EVAL-02）。

## 结果

- 全量评测：全部场景 PASS（3/3），含 xianyu-seller 五维度 18 场景（assembly/explain/guide/tool/hitl/refusal）；
  审计完整性校验 PASS（384 条事件，五段链路齐全）。
- 单元/集成：`pnpm -r build` 绿；`pnpm -r --workspace-concurrency=1 test` 全绿
  （contracts 84 / assembly 76 / server 121 等）；依赖 lint（U2）通过。
- E2E：`pnpm test:e2e:xianyu`（真实 Chromium + MV3 extension + HTTPS 夹具）happy path 与异常 path 全过。
- 行为等价性：原 xianyu-{fulfillment,shipping} 连接器的全部单测场景移植为声明驱动用例
  （`apps/server/test/prepare-intent.test.ts`），输出逐字段一致。

结论：迁移无行为回归；prepare 工具名变更（`prepare_xianyu_*` → `prepare.<toolId>`）已同步
pack prompts 与 mock 剧本，评测通过率不受影响。

## 批次③（pack automations 声明 + 描述符下发 + extension 通用化）

改动面：pack.schema.json 增 `automations[]`（generic 互斥）+ C3 user-message 增可选 `automationId`；
assembly `listAutomations()` + 跨 pack id 查重拒载；服务端新增 `GET /v1/automation-descriptors`（JWT 鉴权）、
自动回合完成帧 toolId 改用 `automationId`；extension 周期扫描按描述符参数化（alarm/工作页/提示词/开关
逐 automation id，legacy 存储键一次性迁移），options 页动态渲染；xianyu-seller pack 0.9.0 声明
`xianyu-auto-scan` automation（根快照 1.3.0）。

结果：
- 全量串行测试绿（extension 130 / contracts 87 / assembly 78 / server 123 等，共 557 用例）；依赖 lint（U2）通过。
- E2E：`test:e2e:xianyu`（Chromium 履约 happy/异常双路径）与 `test:e2e:sidepanel` 全过。
- 全量评测（host-demo + acceptance + assets 三根、每场景 3 跑）全部 PASS；审计完整性 PASS（384 事件）。

已知配套要求：完成帧 toolId 由固定 `xianyu-auto-scan` 改为随 `automationId`（旧插件未升级时自动回合
完成帧关联降级为恢复路径兜底）——extension 与 server 须同批发布（下一次 release 一并出 0.4.0）。
