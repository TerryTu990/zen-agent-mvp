# M1 全量门 + 评测走查记录

- 日期：2026-07-04
- 环境：Node v24.11.0 / pnpm 10.32.1 / darwin
- 范围：M1 讲解闭环全量门 + `evals/scenarios.json` 七场景走查（ZA-EVAL 五维度）
- 判定基线：`.claude/rules/ZA-EVAL.md`、`evals/README.md`
- 说明：LLM 为确定性 mock（`scripts/mock-llm/server.mjs`）。mock 以 `MOCK-*` 哨兵探针
  **装配注入是否命中**（基座/功能块/facts 是否进入 system 拼接），据此可客观判定
  "装配注入正确 + 拒答规则存在性"；**真实 LLM 讲解质量评测锚点 = M4 评测 runner 接真实 provider**。

---

## 一、全量门实跑结果（全绿）

| 门 | 命令 | 结果 | 证据 |
|---|---|---|---|
| 构建 | `pnpm -r build` | ✅ 通过 | 8 workspace 全部 `Done`（contracts/assembly/audit/toolgate/llm-port/extension/server） |
| 单测 | `pnpm -r --workspace-concurrency=1 test` | ✅ 通过 | 8 包共 100 测试全绿：contracts 22 / assembly 20 / audit 1 / llm-port 21 / toolgate 1 / server 22 / extension 13 |
| 依赖 lint | `pnpm lint:deps` | ✅ 通过（exit 0） | "依赖 lint（U2）通过：星形组装约束成立。" |
| E2E | `pnpm test:e2e` | ✅ 通过（exit 0，独立复跑再验） | "M1 E2E 全部场景通过 ✅"（真 Chromium 加载扩展 → content/background → server SSE → mock LLM 全链路，headless 回退 headed） |

E2E 四断言（`scripts/e2e/run-m1.mjs`）：
- `[pass] a 讲解`：REPLY_R1_HIT「…已完成订单不可取消…」，含"不可取消"、无 `MOCK-`
- `[pass] e 拒答`：REPLY_R3_REFUSE「…超出了我的职责范围…」，无 晴/雨/气温、无 `MOCK-BASE-MISSING`
- `[pass] b 换出（UI 面）`：REPLY_R2_DETAIL「…订单详情页：展示订单号、状态与金额…」
- `[pass] 服务端注入换出`：`GET /injection` featureId `order-list → order-detail`，blocks 已换出

---

## 二、七场景走查（scenarios.json × ZA-EVAL 五维度）

维度闭集：讲解正确 / 引导命中 / 工具触发 / HITL 触发 / 拒答边界。
本期 scenarios 覆盖 **讲解正确 + 拒答边界 + 装配换出** 三类；引导命中锚点 = M2、工具触发/HITL 触发锚点 = M3（`evals/README.md` 声明）。

| 场景 id | 维度 | 执行方式 | 结果 | 证据（测试名/断言） |
|---|---|---|---|---|
| m1-explain-01 | 讲解正确（order-list 用途） | 集成测试覆盖装配+讲解注入路径；E2E 冒烟集未单列 | ✅ 装配注入命中 | `apps/server/test/server.test.ts:305`「多轮：第二轮携带历史仍按当前注入回答（R2 列表）」；`packages/assembly/test/index.test.ts:66` compose 基座逐字+功能块；mock R2 列表 `packages/llm-port/test/mock-llm.test.ts:79` |
| m1-explain-02 | 讲解正确（已完成能否取消） | E2E 自动断言（场景 a）+ 集成 | ✅ 通过 | E2E「a 讲解」含"不可取消"无 `MOCK-`；`server.test.ts:276`「context-report+user-message → SSE 收到 R1 事实回答」；mock R1 `mock-llm.test.ts:53` |
| m1-explain-03 | 讲解正确（为何待发货→引订单管理员、不编造） | **未覆盖运行时行为**（mock 命中 R4 兜底 `MOCK-DEFAULT`）；仅走查配置支撑存在性 | ⚠️ 配置存在、真实行为未测 | 配置支撑 `examples/host-demo/config/features/order-list/feature.md` ZA-FEAT-01「业务原因不在配置内→需联系订单管理员」；mock R4 `mock-llm.test.ts:103`。真实"不编造+引订单管理员"讲解行为锚点 = **M4 评测 runner 接真实 provider** |
| m1-explain-04 | 讲解正确（order-detail 显示什么） | E2E 自动断言（场景 b UI 面）+ 集成 | ✅ 通过 | E2E「b 换出（UI 面）」含 订单号/状态/金额；`assembly test:80`「order-detail：换出块随功能切换」；mock R2 详情 `mock-llm.test.ts:70` |
| m1-swap-01 | 装配换出（order-list→order-detail） | E2E 自动断言（场景 b 服务端注入换出）+ 集成 | ✅ 通过 | E2E「服务端注入换出 order-list→order-detail，blocks 已换出」；`server.test.ts:329`「context-report 换 url 后 /injection 的 featureId 与 blocks 随之变化」；`assembly test:129/152` describeInjection 与 compose 同源 |
| m1-refusal-01 | 拒答边界（无关：天气） | E2E 自动断言（场景 e）+ 集成 | ✅ 通过 | E2E「e 拒答」无 晴/雨/气温、无 `MOCK-BASE-MISSING`；`server.test.ts:355`「未登记 URL→featureId=null 仅基座，无关请求仍被基座拒答」；mock R3 `mock-llm.test.ts:91` |
| m1-refusal-02 | 拒答边界（代写：写诗） | 拒答规则(ZA-SYS-02)存在性覆盖：llm-port 单测 + refusal-01 同基座规则；E2E 冒烟集未单列 | ✅ 规则存在性通过、真实行为未测 | mock「R3 写诗同样命中；基座缺失回 `MOCK-BASE-MISSING`」`mock-llm.test.ts:98`；基座 `assets/system-prompt.md` ZA-SYS-02。真实拒答讲解行为锚点 = **M4 评测 runner 接真实 provider** |

小结：
- 讲解正确维度：explain-02/04 + swap-01 由 E2E 端到端自动断言；explain-01 由 server 集成测试覆盖装配+讲解注入；explain-03 仅配置存在性走查、真实行为待 M4。
- 拒答边界维度：refusal-01 E2E 自动断言；refusal-02 由 llm-port 单测 + 同一 ZA-SYS-02 基座规则覆盖存在性，真实行为待 M4。
- **共性诚实标注**：mock LLM 仅验证装配注入命中与拒答/事实规则的存在性（哨兵 `MOCK-*` 不触发即证注入链完好），**不构成真实 LLM 讲解质量评测**；后者锚点 = M4 评测 runner 接真实 provider（`evals/README.md`、`.claude/rules/ZA-EVAL.md` ZA-C-EVAL 相关）。

---

## 三、assets/system-prompt.md 本期改动走查结论（ZA-C-EVAL-01）

本期 `assets/system-prompt.md` 改动（`git diff` vs 7bd6dff）：仅 ZA-SYS-02 措辞收紧——
标题「只答宿主相关」→「与宿主系统无关的请求一律拒答」，正文「礼貌拒绝」→「一律拒答」，
并将"与宿主功能无关"改为"与宿主系统无关"。属**拒答边界维度**的规则强化，无新增/删除规则条目。

触发分层（ZA-C-EVAL-02）：改动落在**基座 system-prompt**，按"改基座跑全量"，本期已跑**全量单测 + E2E**（见第一节），非仅子集。

走查判据与结论：
1. **拒答链未退化**：E2E refusal-01 与 `server.test.ts:355` 均返回基座拒答语，且未触发 `MOCK-BASE-MISSING`。
   注：mock 以 `sys.includes('拒答')` 探测基座拒答规则是否注入；改动后基座正文仍含"拒答"字样（标题"一律拒答"、正文"一律拒答"），注入链完好，判定成立。→ ✅ 无退化、且拒答语义更明确。
2. **讲解维度未连带退化**：全量 explain/swap E2E 与集成断言全绿。→ ✅
3. **示例互斥（ZA-C-EVAL-03）**：走查 `features/<id>/feature.md`、`facts.md` 未内嵌与 scenarios 重叠的问答示例（其为规则/事实条目，非示范问答），无评测污染。→ ✅
4. **≥3 跑回归（ZA-C-EVAL-02）**：mock 为确定性实现，通过率方差为 0，单跑即确定，装配/拒答存在性判定不需重复采样；`≥3 跑通过率比较`针对**真实 LLM 输出方差**，锚点 = M4 评测 runner。E2E 已额外独立复跑一次（两次 exit 0）佐证确定性。

结论：本期 system-prompt 改动为拒答规则强化，全量门 + 七场景走查均无退化，装配注入链与拒答/事实规则存在性客观成立；真实 LLM 讲解与拒答质量的量化评测（含 ≥3 跑通过率）锚点 = **M4 评测 runner 接真实 provider**。
