# G6 批次评审记录：E2E 门 + 发布裁定

> 批次：G6（L0-L3 实施方案 §1 末批）。日期：2026-08-05。
> 流程：E2E 驱动三路并行（workflow）→ 通过门核对（独立 agent，不采信自报、自行复跑）→ 缺陷修复 →
> 三视角只读评审 → 本记录 + commit → 按门裁定发布与否。
> 验证基线（五轮修复后终态）：`pnpm -r build` 0 错；`pnpm -r --workspace-concurrency=1 test` 全绿 **866 用例**
> （extension 237 / server 190 / contracts 178 / assembly 113 / toolgate 93 / 其余 55）；`pnpm lint:deps` 通过；
> `assets/` 零改动（G 线全程 `git diff --stat eea555f..HEAD -- assets/` 为空），六维评测不触发。

## 1. 批次范围

- **E2E 驱动落地**（`scripts/e2e/`）：`run-g6-explain-pack.mjs`（A+D）、`run-g6-user-config.mjs`（B+C）、
  `run-g6-automation.mjs`（F）、`run-g6-real-site.mjs`（E，**骨架，从未执行**）+ `fixtures/g6/`（三态快照夹具，纯数据）。
  范式沿既有 `run-xianyu.mjs`：真 Chromium + MV3 extension + 真 gateway/assembly + 仓库自带确定性 mock LLM。
  harness 签名密钥改为**每次运行进程内随机生成**，仓库不留固定夹具串。
- **缺陷修复五轮**：浏览器级实测发现两处（§2）→ 三视角评审 7 major + 11 minor（§4.1）→ 同视角复核四轮逐轮收敛（§4.3–§4.6）。
- 测试：基线 825 → **866**（+41）；新增 `apps/server/test/watch-run-pure.test.ts`，并给两类手抄镜像补了防漂移守卫。

## 2. 缺陷发现与处置

| # | 级别 | 发现 | 处置 |
|---|---|---|---|
| B1 | **blocker** | **`automationId` 未随上行帧送达服务端**：`background.ts` 的 `case 'auto-scan'` 只映射 `automationRunId`，`frames.ts` 的 `UserMessageFrame` 无该字段（契约 TS+schema 有、`gateway.ts:2110` 读它）。后果：每次自动回合都被服务端当**普通回合**——不解析 watch、不强制只读工具面、不做快照比对与报告收口，**G5/adr-021 的 R7 结构强制从真实客户端整条不可达**（有 pack 的站点上等于无人值守轮拿到完整工具面）；完成帧 `toolId` 回落 `'automation'` 使 `isAutoScanCompletion` 恒不匹配、单飞锁悬挂（实际每两轮才跑一轮）。缺陷引入于 `d4a51c8`（adr-019 批次③），非 G5 引入，且已随 0.3.4 发布 | 上行帧投影下沉到 `auto-scan.ts` 的 `autoScanUpstreamFrame`（该模块已有单测覆盖），`background.ts` 改调它；补单测断言两个 id 同时上行 |
| B2 | major | **存储降级轮的透明视图对用户完全沉默**：`injection-view.ts:renderTools` 按 `description.toolIds`（可见面）驱动工具行，而降级时 compose 清空 `toolIds`、只在 `tools` 保留全量 `effectiveTier=forbidden` + `tightenedBy='storage-failure'` → 降级轮工具行数实测 = 0，`'因存储故障降级收紧'` 分支**在面板中不可达（死代码）**。用户看到「agent 突然什么都不做」且无任何解释，违 R4/R6 | 行集改为「可见面 ∪ 逐项描述」，落面工具成行并标 `data-za-tool-unavailable`；header 增降级横幅说明「工具面整体禁止、讲解与引导仍可用」；补真实降级形态用例（旧用例保留了非空 `toolIds`，故未暴露——属假绿） |
| B3 | minor | `client-access-layer.schema.json:71` 的 `$comment` 与 TS 投影仍写「解析不到按普通回合处理」，而 G5-M1 修复后实现已是 fail-closed（未知实例 403 / 降级快照 503）——**契约文本比实现宽** | schema `$comment` 与 TS 注释同步改为「一律拒绝该轮，不回落普通回合」，并写明回落即失守的原因 |
| B4 | minor | HITL 确认卡不标注「因你自己的收紧而弹出」（无 `baseTier→effectiveTier` 与 `tightenedBy`）——R4 来源标注在注入视图有、在裁决现场无 | 未修，登记 deferral（§4） |
| B5 | minor | 旧品牌残留：`conversation-hitl.ts:59` 助手名 `'Zen Commerce'`、`sidepanel.ts:45` 空态硬写「闲鱼卖家后台」、`manifest.json` name | 已在 `docs/plans/2026-08-04-generic-extension-productization.md:93` 登记为有锚点 deferral（锚点：P2 品牌回归），不重复登记 |
| — | 观察 | 单个 pack 的 `engines` 不兼容导致**整快照拒载**，同 registry 内健康 pack 一并不可用。与 adr-020 fail-closed 一致，但社区 pack 分发场景下「一个坏 pack 拖垮全站」值得评估 | 非缺陷，登记 deferral（§4） |

## 3. E2E 案例集执行结果（方案 §6）

| 案例 | 结果 | 跑次 | 证据 |
|---|---|---|---|
| A 讲解与拒答 | ✅ 通过 | ≥4（含修复后回归 2） | `e2e-evidence/e2e-a/`：a1/a2 各 png + sse-frames.json + audit.jsonl + case.json |
| B L2 全链路 | ✅ 通过（8 条断言） | ≥4 | `e2e-evidence/e2e-b/`：b1-b4 五张 png + audit-e2e-b.jsonl + assertions.log |
| C 治理故障语义 | ✅ 通过（4 条断言） | ≥4 | `e2e-evidence/e2e-c/`：c4/c5 png + audit-e2e-c.jsonl + injection-view-degraded.txt |
| D pack v2 载入 | ✅ 通过（D1/D2/D3） | ≥4 | `e2e-evidence/e2e-d/`；`d2-server-stderr.txt` 为拒载可定位错误原文 |
| **E 真实站点主案例** | ❌ **BLOCKED（判未通过）** | **0** | 无 —— `e2e-evidence/e2e-e/` 不存在 |
| F 通用自动化 R7 | ✅ 通过 | 3（修复后 2） | `e2e-evidence/e2e-f/`：result.json（`denials=1 / unattendedReadOnly=true / writeToolExecutions=0 / hitlCards=0`）+ 两张 png |

**E2E-E 阻塞原因（如实）**：该案例需真实 LLM 密钥与飞书运行时凭证；凭证位于测试根 `.env`，
而 `za-secret-guard`（ZA-C-SEC-03 凭证读禁区）拦截任何指向该文件的装载命令，开发期不得绕过
（SEC-03 明示解释器拼接属已知残余面、仍须自守）。故本批只产出驱动骨架与运行口径，**未执行、未伪造通过**。
解除条件与运行命令见 §5。

## 4. 三视角评审与修复轮

首轮发起（15:50）三路 + G5 补做复核三路**全部因会话限额中断**，零结论；18:20 限额重置后重新发起，六路全部完成。

### 4.1 G6 三视角发现（无 blocker；7 major + 11 minor，已全部处置于 `b87ff87`）

| # | 级别 | 视角 | 发现 | 处置 |
|---|---|---|---|---|
| A1 | major | 架构 | **只带 `automationRunId` 的帧绕过整段 watch 归属判定**：`gateway.ts` 入口条件是客户端是否自愿声明 `automationId`；任一客户端省略该字段即以普通回合拿到完整工具面（含 hitl 档且无人可确认）。schema 亦无耦合约束 | schema 加 `dependentRequired`；网关前置 400 拒绝；两 id 绑成 `automationRun` 值，`?? 'automation'` 兜底随之删除 |
| A2 | major | 架构 | **手抄镜像唯一守卫只比对帧 type 闭集，从不比对属性集**——本批 blocker 正是它漏掉的实例——发现时全量单测（当时 827 条）全绿、build 0 错，只有浏览器级才暴露 | `frames-schema.test.ts` 补 14 个帧的 `Record<keyof T, true>` 逐帧对账（编译期穷举 + 运行期与 schema 属性集比对）。**已实证可拦**：删掉 `frames.ts` 的 `automationId` 后 `tsc` 报错 |
| A3 | minor→重构 | 架构 | fail-closed 整块包在 `deps.userConfig !== undefined` 内，拒绝面随组装方式漂移 | pack 声明自动化改由 `assembly.listAutomations()` 判定，与 L2 存储装配解耦；无存储且非 pack 自动化 → 403。**此修复暴露了 `server.test.ts` 三条自动化用例本就跑在未装配 L2 的形态下**（即评审 N2 所指），重构后恢复全绿 |
| A4 | minor | 架构 | `watch-run.ts` 的 `none` 注释仍写「未知 id 按普通回合处理」，与实现的 403/503 相反 | 改为「未知 id 由调用方 fail-closed 拒绝」 |
| A5 | minor | 架构 | E2E-E 的 `TEST_ROOT` 是开发机绝对路径且无覆盖入口——发布门脚本只有原作者机器能跑 | 改 `ZA_E2E_TEST_ROOT ?? <repo>/.za/e2e`。其余三脚本仅默认值不可移植、有 env/CLI 覆盖，未改 |
| P1 | major | 产品 | **「本轮不可用」对所有落面工具无条件施加**：用户自己长期关停的工具被说成只影响本轮，且该路径 `isStorageDegraded=false` 无横幅澄清 | 按 `tightenedBy` 分叉：storage-failure → 「本轮不可用」；其余 → 「已被你的定制关闭」 |
| P2 | major | 产品 | **E2E-C 的 C5 是「观察记录，非断言」**——B2 缺陷在浏览器级零回归守卫，服务端降级 compose 变形会让工具行数回到 0 而两级测试双绿 | 升为三条断言（工具行数 >0 / 收紧来源标注 / 降级横幅） |
| P3 | major | 产品 | **E2E-E 判定口径④「面板清单条目数 == N」完全缺失**，①比的是常量而非实际提取数——模型少提或编造补足会判通过 | 新增 `countNumberedItems` 从面板正文数编号行；断言提取数 == N 且表格新增行数 == 实际提取数 |
| P4 | major | 产品 | **E2E-E「HITL 确认先于写入」判据不成立**：三条断言对「一次确认放行五次写入」与「逐次确认」完全不可区分；且「出现 deny 即失败」会把一次正确的 fail-closed 拒绝算作案例失败，方向上鼓励放宽工具面 | 改为按审计流逐条配对（`approvals.length >= executions.length` + 逐条 `ts` 先后）；移除 deny 判负 |
| P5 | major | 产品 | **`automationId` 送达后服务端拒绝成为可达分支，而插件丢弃投递结果**：一次瞬时存储抖动 → 503 → 锁悬挂 → 下周期恢复判定把用户触发器写成 `enabled:false`，两条提示都无法让用户判断该不该重开 | 改用 `deliver` 取 httpStatus 分叉：403 暂停并给可行动文案；503 只释放锁、不动用户配置、下周期重试 |
| P6 | minor | 产品 | 降级轮 L2 分区空态说「本轮无该层注入段」——「无」与「读不到」是两回事，用户可能据此重新录入规则 | 降级时改为「本轮读取失败，未注入（你的定制未被清空）」 |
| P7 | minor | 产品 | 基线本就 forbidden 的工具被标「因存储故障降级收紧」——它并没有被收紧 | 收紧来源只在 `effectiveTier !== baseTier` 时显示 |
| P8 | minor | 产品 | E2E-E 的写入 oracle 用 `innerText` 数标记，飞书若 canvas/虚拟滚动渲染则恒 0（fail-closed，不假绿，但案例被永久堵死） | 补进骨架未验证点清单，给出备选 oracle |
| R1 | minor | 规则 | **`assertions.log` 声称验证「低于基线档位不可选」而实际断言恒真**（基线 auto 的工具上「auto 可选」永真），被高估的正是 R1 只收紧这条治理面 | 改用基线 hitl 的 `order-list.cancel-order` 断言 `option[value="auto"]` 为 disabled + forbidden 可选 |
| R2 | minor | 规则 | injection-view 新用例题为「降级轮真实形态」却沿用夹具默认的两个 L2 blocks——L2 分区断言由夹具遗留撑住，非由降级逻辑撑住 | 用例改为真实降级形态（L2 blocks 为空），并断言降级空态文案 |
| R3 | minor | 规则 | `run-g6-user-config.mjs:37` 注释写「固定夹具值」而实现已改随机——直接误导 SEC 姿态判断 | 与其余三脚本表述统一 |
| R4 | minor | 规则 | E2E-E 是唯一将带真实凭证运行的驱动，却脱敏最弱（只查 token，不查 harness 密钥、不统一 redact） | 加 `redact()`（两个 harness 密钥 + `ZF_LLM_API_KEY` + JWT 三段式）覆盖所有落盘 |
| R5 | minor | 规则 | 同 P2（观察非断言）——独立复现，已并案 | 同 P2 |

### 4.2 G5 同视角复核（补做，方案 §5 要求的复核在此了结）

| 条目 | 产品 | 架构 | 规则 | 合并裁定与本批处置 |
|---|---|---|---|---|
| M1 未知 automationId fail-closed | RESOLVED | RESOLVED | PARTIAL | **PARTIAL**：判定本体成立，但三路一致指出——G5 提交时该分支在真实客户端上是死代码（`automationId` 未上行），故 G5 记录 §4 的 ✅ 在服务集成级为真、产品级为假，且 0.3.4 已带该失效面发布。残余（依赖可选注入、两条主路径零用例）已于本批 A3/19 修复 |
| M2 基线推迟到报告成功后 | RESOLVED | RESOLVED | RESOLVED | **RESOLVED**。规则路指出「同一变化下轮仍会被检出」只在服务端成立（客户端失败轮会停用触发器），已由本批 P5 的 503 分支缓解 |
| M3 观测面纳入 notices + 限制披露 | PARTIAL | RESOLVED | PARTIAL | **PARTIAL → 本批修复**：机制成立，但处置表称的三处披露只兑现 adr 一处，两处面向用户的文案都没写；面板 hint 与模板 description 已补「不含普通段落正文」 |
| M4 文案不实 | RESOLVED | RESOLVED | RESOLVED | **RESOLVED** |
| M5 watch 跳过自我关停 | PARTIAL | RESOLVED | RESOLVED | **PARTIAL → 本批修复**：原路径已闭合，但自我关停另有四处入口，且 M6 的服务端收紧恰好把一个高频正常场景送进其中一条（见下） |
| M6 基线归并 + 工作页精确匹配 | PARTIAL | PARTIAL | PARTIAL | **三路一致 PARTIAL → 本批修复**：见 N1/N2 |

**G5 复核的新发现（本批已修）**

| # | 级别 | 发现 | 处置 |
|---|---|---|---|
| N1 | major | **客户端前缀匹配 × 服务端精确匹配 → 触发器被永久暂停**：M6 只收紧了服务端，客户端派生 watch 描述符仍用 pack 的前缀语义。用户在被监测页的子路径多开一个 tab（商品页→评论页），watch 即在下一周期自杀；监测站点首页（路径 `/`）时更是常态而非竞态。且该前缀语义被用例固化为期望行为 | `isAutoScanWorkPage` 对 watch 描述符改路径精确相等，pack 保持前缀；两条固化错误期望的断言改写，并补 pack 前缀语义未受影响的用例 |
| N2 | major | **基线键用 `snapshot.url`（含 query/hash）而非被监测页**：同一页的跟踪参数变体各自建基线，每轮都命中「首轮建基线」→ **永远不报告，且与真无变化不可区分**——正是 M3 想消灭的失真形状，被 M6 以另一种方式重新引入 | 新增 `watchPageKey` 归一（origin + 去尾斜杠路径），基线键改用 `watchPageKey(watch.url)` |
| N3 | major | **M1/M2/M6 三条修复零回归锁**；`watch-run.ts` 自述「只做可独立验证的确定性计算」，却一个纯函数单测都没有，全部经 HTTP 集成路径间接触达 | 新增 `apps/server/test/watch-run-pure.test.ts`（9 条）+ `watch-run.test.ts` 补两条集成用例 |
| N4 | minor | G5 记录 §1 的用例增量数字不实：称 +43（extension 16），实测 extension +22，总数应为 +49（低报） | 本记录如实登记；G5 记录不追改（历史记录保持原样，更正见此） |
| N5 | minor | `automation-template.ts` 周期上界注释仍写「超过一天的周期用调度已无意义」，是 1440 上界时代残留 | 改为与调度端同值的表述 |
| N6 | minor | adr-021 两处仍描述修复前语义（「解析不到则按普通回合处理」「path 前缀」）——该批次决策 SSOT 失准 | 两处改写，并补基线归并语义 |
| N7 | minor | 面板通知「任一不确定状态即暂停」横跨 pack 与 watch 两组，M5 后对 watch 已不成立 | 按来源分述改写 |

### 4.3 同视角复核第一轮（对 `b87ff87`）→ 第二修复轮 `a3d7a9e`

三视角各自复核自己提出的条目。**6/8 RESOLVED，2 条未成立，另查出 5 条新引入问题**——其中一条是把修复前就错的谓词原样搬进了新逻辑，验证了「修复本身可能不成立」这一风险是实的。

| # | 复核裁定 | 问题 | 第二轮处置 |
|---|---|---|---|
| 11 | **UNRESOLVED** | E2E-E 的 HITL 配对过滤 `tool-decision.verdict === 'approve'`，而该字段闭集是 `allow\|hitl\|deny`——`approvals` 恒空，第一条断言恒失败，逐条配对永远走不到。修复前的 `.some(...)` 同样恒假，只因案例从未执行而未暴露 | 改为 `hitl-verdict.decision === 'approve'`（人审放行）∪ `tool-decision.verdict === 'allow' && riskTier === 'hitl'`（toolgate 任务级授权复用）；配对基准收窄到 `outcome === 'ok'` 的执行 |
| 10 | **PARTIAL** | `countNumberedItems` 与面板 markdown 渲染器互斥：`1. ` 被渲成 `<ol>`，而 `innerText` 不含 `<ol>` 序号 marker → 标准输出形态下恒 0，断言以「模型少提或多报」的错误归因硬失败（产品路以 playwright 实测确认） | 改 `countListItems(panel)`：数最后一条 `.za-msg` 内的 `ol>li, ul>li`，无列表时回退按行首编号；取数点从写入轮之后移到提取轮结束 |
| 12 | **新引入 major**（产品/规则一致） | 「只有 503 重试」写成了「除 503 外全部暂停」：网络不可达无 `httpStatus`，401/404/409 亦然 → **一次断网永久关停用户的监测**，而修复前该路径经 `decideAutoScanRecovery('unavailable')` 判 `keep-busy`。是回归 | 抽 `decideAutoScanDelivery` 纯判定：**只有 403** 动 `enabledKey`，其余一律下周期重试；补单测锁定（两轮内反向改两次且此前零覆盖） |
| — | **新引入 major**（产品/架构一致） | `watchPageKey` 丢弃整个 query/hash → 分页/筛选/hash 路由共用一条基线，每轮互相 diff 出**整页虚假变化**；且客户端按 `tab.url` 选页、服务端按快照时 `location.href` 判定，两时点间任何漂移都翻转判定 | 归一改为保留查询串与 hash，只剥离纯广告点击标识（`utm_*` + `gclid/fbclid/msclkid/yclid`）；两端手抄同一算法 |
| — | 新引入 minor（架构） | 归一解码后重拼且不再编码 → `?b=%26c%3Dd`（单参）与 `?b=&c=d`（两参）产出同一 key，重演它要治的病 | 拼接前 `encodeURIComponent`（两端同改） |
| — | 新引入 minor（架构） | `utm_` 前缀判定区分大小写而闭集查表不区分，`UTM_source` 变体被判成另一个页 | 统一小写后判定 |
| — | 新引入 minor（架构） | 两端手抄归一函数**无防漂移守卫**，改单边两边全绿而线上分歧 | `watch-run-pure.test.ts` 加源文本对拍（函数体 + 闭集逐字相同） |
| — | 残余（产品/规则一致） | 「本轮不可用」对基线即 forbidden 的工具仍不自洽；`FRAME_PROPERTY_MIRRORS` 无覆盖自检；四处注释被本轮语义反转推翻（含 adr-021 两处、`watch-run.ts` JSDoc、E2E-E 头部口径③）；另三个 E2E 脚本证据默认路径仍是开发机绝对路径 | 逐条改写；覆盖自检补断言；证据默认路径统一为 `<repo>/.za/e2e/`（`.gitignore` 已覆盖） |

### 4.4 同视角复核第二轮（对 `a3d7a9e`）→ 第三修复轮

**8 条中 6 条 RESOLVED**。剩余问题收敛到一条结构性缺陷与若干注释/文案：

| # | 裁定 | 问题 | 处置 |
|---|---|---|---|
| N1 | **新引入 major**（产品/架构一致） | 判定面收紧后接上了既有的「失败帧即永久停用」通路：`isWatchWorkPage` 判否 → `settle('error')` → 失败完成帧 → 客户端把触发器整条停用。用户在派发与取快照之间点一下页内链接、或站点回写参数，触发器就自杀 | **从根上改**：判否不再按失败收尾——它不是故障。当轮改记 `ok`，第四轮进一步改记 `skipped`（§4.5：记 `ok` 会让「没看成」冒充「看过没变」）|
| 3 | PARTIAL | 停用面还有两处同类门（`forward(contextMessage)` 失败、任何失败轮） | 前者属既有代码、本轮未触；后者已由 N1 的修法从源头消解（判否不再产失败帧）。剩余「真失败即停用」是有意语义，登记 deferral |
| 1/2 | RESOLVED（带残余） | 谓词与 oracle 本体成立，但 E2E-E 从未执行、仍是纸面推演；`countListItems` 对「模型输出表格 / 整轮多个列表」会退化为 fail-closed 失败 | 残余写入骨架未验证点清单，随 E2E-E 首跑裁决 |
| 7 | PARTIAL | `watch-run.ts` JSDoc、`auto-scan.ts:152`、测试 describe、E2E-E 口径③前半句仍是旧语义；adr-021 §4 被本轮语义反转再次打破 | 五处逐条改写 |
| 4/5/6/8 | RESOLVED | 两端逐字段等价（架构路以 18 条语料实跑差分，0 处不等）；无遗漏消费方；「一直不可用」分叉穷举无误标路径；覆盖自检为全等比对 | — |

**第三轮验证**：`pnpm -r build` 0 错；全量测试 862 用例全绿；`lint:deps` 通过；E2E-A/B/C/D/F 五案例逐条实跑通过。

### 4.5 同视角复核第三轮（对 `f11552e`）→ 第四修复轮 `832aa39`

三路一致把「判否改 `settle('ok')`」判 **PARTIAL**：改动确实断了「一次页内点击杀死触发器」的通路，但把「本轮没看到被监测页」记成了 `ok`——而 adr-021 §4 明写该审计行是运行历史「无变化」行的数据源，等于把「没看」渲染成「看过没变」（R6）。前两轮把「漏报」换成「误报」，这一轮换成了「伪报成功」。

| 处置 | 内容 |
|---|---|
| C5 闭集加 `skipped` | `ExecutionOutcome` 与 schema enum 同步扩员，`$comment` 写明 MUST NOT 与 `ok` 合并统计；判否改记 `skipped`。`settle` 对客户端仍按成功收尾——单飞锁释放、不停用、不打扰面板三条语义不变 |
| 畸形上报与切页分开 | 上报 URL 不可解析改判 `error`（那是真故障，不是用户切了页） |
| 补该分支的回归锁 | `driveWatchRun` 加 `reportUrl`；新增集成用例断言 outcome 序列与基线未被污染。三路一致点名「同一决策点三轮内翻三次而零覆盖」 |
| `spm` 放回跟踪参数闭集 | 产品路指出它是阿里系逐次轮换的位置埋点，而本项目旗舰站属阿里系——移出会让这些站点的 watch 每轮判否，且因跳过轮静默而无从察觉 |
| 守卫自证、面板与 403 文案、记录矛盾 | 逐条改写 |

### 4.6 同视角复核第四轮（对 `832aa39`）→ 第五修复轮

条目 1/2/4/5 三路一致 **RESOLVED**（含逐条核实契约同构、用例可证伪、客户端处置链）。剩余全为 minor，本轮处置：

| 发现 | 处置 |
|---|---|
| 「基线未被污染」断言无鉴别力——污染与否结果相同 | 第三轮改报与首轮相同内容并断言判「无变化」，再加一轮验证真变化仍检出（现为 ok/skipped/ok/ok 四轮） |
| `skipped` 在 schema 侧零覆盖——删掉 schema 那一行全量仍绿 | `schemas.test.ts` 合法事件补 `tool-execution skipped` 正例 |
| `spm`/`ref`/大小写/编码碰撞四条归一行为均无用例 | `watch-run-pure.test.ts` 逐条补断言 |
| 「设置页」不指向任何实际页面（触发器开关在「自动化」页） | 403 分支、失败轮、`shouldPauseAutoScan` 三处改「配置中心「自动化」页」；报告失败轮随本轮补齐，余下三条停用提示仍缺，见 §6 |
| 畸形上报的 error 落在无恢复路径的文案上 | 同上一并补 |
| adr-021 未随实现更新（`spm` 归类、C5 闭集扩员及其对读方的不同要求） | 两处补写 |
| 本记录多处与代码不符（计数、用例数、轮次、deferral 文本） | 本次一并修正 |

**终态验证**：`pnpm -r build` 0 错；全量测试 **866 用例全绿**；`lint:deps` 通过；E2E-A/B/C/D/F 逐条实跑通过。

按方案 §5，G6 的 blocker/major 已全部处置并经同视角复核（四轮）；剩余为带锚点的 minor 与残余（§6）。**该门项闭合**；发布门仍因 E2E-E 不绿（§5）。

## 5. 发布裁定

# 🔴 不发布

方案 §7 发布前门四项中**一项未满足**：

1. **§6 通过门未满足** —— E2E-E 通过次数 0，方案要求 ≥3。它是唯一同时覆盖「真实 DOM 快照可用性」
   「跨站会话组」「真实凭证下的 HITL 写入」的案例；A/B/C/D/F 全部跑在本地夹具 + mock LLM 上，
   **对真实站点可用性零证据**，无可替代。
2. ~~三视角 review 门未闭~~ —— **已闭合**：六路评审 + 四轮同视角复核全部完成，blocker/major 全部处置（§4.1–§4.6）。

绿灯项（已亲自复跑）：全量 build / 串行 test **866 绿** / lint:deps；E2E-A/B/C/D/F；G 线 `assets/` 零改动。

**解除阻断的最小路径**（按依赖序）：
1. ~~重跑三视角评审~~ —— 已完成（§4）。
2. 由 Terry 提供真实 LLM 凭证与已登录 goofish/飞书的持久化 profile，跑通 E2E-E ≥3 次并归档证据：
   `node --env-file=<测试根>/.env scripts/e2e/run-g6-real-site.mjs --runs=3`
   （另需 `ZA_E2E_FEISHU_SHEET_URL` 指向 zen-agent-test 文件夹内目标表格、`ZA_E2E_PROFILE_DIR` 指向已登录 profile）；
3. `apps/extension/manifest.json` 版本递增（当前仍为上次发布的 0.3.4）后再走 release skill。

**若 E2E-E 因外部依赖确实不可达，MUST NOT 自行降级判绿**——须由 Terry 就「E2E-E 未验证状态下是否接受发布」
显式确认（ZA-C-WHEN-01 唯一放行口径）。

## 6. 未了结 deferral（WHEN-01，全部带锚点）

### 本批新增

| 项 | 锚点 |
|---|---|
| **E2E-E 真实站点主案例未执行**（骨架已在仓，通过次数 0） | **发布前必须了结**，依赖 Terry 提供真实凭证与已登录 profile；不可顺延 |
| E2E-E 骨架两个未验证点：闲鱼搜索页/飞书表格页要素能否被通用 DOM 快照定位；若不足须为飞书表格补 pack（走 L1 载入校验），**不得放宽 generic 工具面** | E2E-E 首次实跑时裁决 |
| E2E-E 骨架当前 `snapshotRoot` 指向 `examples/acceptance`（`assets/` 内无 generic 兜底 pack），即走的不是发布快照 | E2E-E 实跑前裁决：补 pack 进 `assets/` 或接受示例快照 |
| ~~G6 三视角评审 + G5 同视角复核未完成~~ | **本批已了结**（§4.1/§4.2 六路结论；§4.3–§4.6 四轮同视角复核） |
| E2E-E 的 `countListItems` 对「模型输出 markdown 表格」或「整轮多个列表」会退化为 fail-closed 失败 | E2E-E 首次实跑时裁决 oracle |
| `background.ts` 的自我停用还有两处入口未按「瞬时故障不停用」收口：`forward(contextMessage)` 失败、真失败轮；另有三条停用提示无恢复指引（恢复判定 `release-and-pause`、上下文同步失败、页面操作中止），pack 自动化自我关停完全无通知 | 下一次改插件调度链路时（与「`triggerAutoScan` 忽略投递结果」同批） |
| 跟踪参数闭集只收 `utm_*` + `gclid/fbclid/msclkid/yclid/spm`；站点自有来路参数（`from=`/`scm=`/`share_source=`）仍会使 watch 每轮判否，而跳过轮不打扰面板 → 用户看到的是长期沉默 | 触发器运行可见性落地时（届时跳过轮在运行历史可见，用户能发现并反馈；同期评估闭集扩充或在建触发器时回显归一后的地址） |
| `pack.schema.json` 的 `workRoutes` `$comment` 仍只描述 pack 前缀语义，而该字段现有两个语义不同的生产者 | 下一次改 pack 自动化声明契约时 |
| HITL 确认卡不标注收紧来源（B4） | 并入「配置中心可用性收口」批次（见下） |
| 单个 pack `engines` 不兼容导致整快照拒载，无 per-pack 隔离降级 | 第三方/社区 pack 分发链路落地时（adr-020 P3.5 pack 导入/升级） |
| 手抄镜像漂移守卫：**帧属性集与归一算法两处已了结**（`frames-schema.test.ts` 逐帧对账、`watch-run-pure.test.ts` 源文本对拍）；`auto-scan.ts` 的 `WATCH_TEMPLATE_IDS` / `PLATFORM_MIN_WATCH_MINUTES` / `MAX_WATCHES` / `WATCH_FOCUS_MAX_LENGTH` 四处常量镜像仍无守卫 | 下一次改这四个常量或其 contracts 侧来源时 |
| 插件无「打开配置中心」的导航入口（全仓无 `chrome.runtime.openOptionsPage`），且侧边栏叫「扩展设置」、停用提示叫「配置中心」两套叫法 | 配置中心可用性收口批次 |
| 服务端未启用用户配置存储时，配置中心自动化页因 `loadError` 早退——既有 watch 行与 pack 分组都不渲染，而「新建触发器」按钮仍在 | 配置中心可用性收口批次 |
| C5 `ExecutionOutcome` 与 schema enum 之间无全等对拍：本批新增的 `skipped` 成员两向可拦（TS 侧经 `tsc`、schema 侧经 `schemas.test.ts` 正例），但**再扩成员时只改单边不会红** | 下一次 C5 闭集扩员时 |
| `decideAutoScanDelivery` 只吃 `httpStatus`，而三个不同 HTTP 调用的 403 塌成同一信号；前置代理在建会话/SSE 端点回 403 会以错误说辞停用触发器 | 部署形态引入前置代理（或 `MessageDeliveryFailure` 需要区分来源）时 |
| 跳过轮（`outcome: 'skipped'`）尚未在面板运行历史上呈现——该视图本身也还没实现 | 随「触发器运行可见性」一并落地（配置中心可用性收口批次） |

### 锚点重挂（ZA-C-WHEN-01：原锚「G6 前面板打磨」已失效，不得再笼统续挂）

以下六项自 G3/G4 起连续 2-3 批次以「G6 前面板打磨」滚动顺延，该锚点随 G6 结束而失效。
现统一重挂到**「配置中心可用性收口」批次**——定义登记于
`docs/plans/2026-08-04-l0-l3-orchestrated-implementation.md` §9，触发条件：**首个外部用户试用前**
（这些项全部是「用户能不能自己看懂并改对配置」，无外部用户时不构成损失；一旦有外部试用即全部必修）。
六项均不破坏治理边界（无一涉 U1-U8 / R1 只收紧 / R7），故不阻断本批。

| 项 | 来源批次 |
|---|---|
| 越界引用（toolId 已不在 L1 工具集）标红 + 清理入口 | G4 |
| pack 能力面 chips 与 `workRoutes` 投影 | G4 |
| 409 冲突后保留待保存态并差异高亮 | G4 |
| 未安装 pack 作用域的 `packConfig` 使后续保存恒 400 且无删除入口 | G4 |
| config-draft 409 提示重复、accept 成功的下行确认帧 | G3 |
| `disabledTools` 表达被保存改写为 `riskTierRaise:'forbidden'`（可见性意图降级） | G4 |
| 触发器运行可见性（每条 watch 的「上次运行 / 结果」） | G5 |
| HITL 确认卡标注收紧来源（本批 B4） | G6 |

### 锚点已到期但本批未处置（重挂）

| 项 | 新锚点 |
|---|---|
| 共享 `tokens.css`：配置中心与侧边栏令牌同源 | 配置中心可用性收口批次 |
| `config-draft-unknown-tool` 分支与 PUT 字节上限分支补专用用例 | 下一次改 L2 写入通道时 |
| reject 草稿弃置的审计留痕（当前无事件） | 下一次审计事件契约修订时（C5 扩字段） |
| 模型臆造 packId 的噪音评估 | 下一次 `assets/` 改动触发六维评测时 |
| `automationRunId`/`automationId` 审计归因只落 watch 轮，pack 自动化轮未透传 | 同上一条（审计事件契约修订） |
| watch 自动回合审计仍以 `tool-execution`+`execution:'server'` 承载（污染 server 通道统计） | 同上（评估新增 `automation-run` 事件类型） |
| `triggerAutoScan` 忽略投递结果 → 单飞锁悬挂到下一周期并触发暂停 | 下一次改插件调度链路时 |

### 锚点未到期（备查，本批不阻断）

`GET /v1/packs` 按 `claims.tenant` 过滤（P4 托管形态）、`readBody` 无流式上限（部署加固批次）、
变化检测正文级比对（首个需要它的模板立项时）、比对基线持久化与多实例语义（托管形态多实例上线时）、
watch 与 pack automation 撞名两端统一（P3.5 pack 导入/升级链路）、test 目录 typecheck 门（CI 门禁就位时）、
L2 导出 / subject 迁移 / Google 登录（P3.5 / P4）、preparation/workflow 服务端硬编码真泛化（第二个履约站点 pack 接入时）、
品牌文案归一（P2 品牌回归）。
