# Phase 0 独立复审记录

## 审查对象

- Phase commit：`a9d48a2 docs(plan): define zen commerce agent delivery`
- 架构 reviewer：`phase0_arch_review`
- 测试 reviewer：`phase0_test_review`

## 初审结论

- P0：0
- P1：架构 5 项、测试 5 项
- P2：测试 3 项

主要问题：活动快照被直接覆盖、容器内飞书认证无持久挂载契约、两个不可逆副作用之间缺持久恢复状态、聊天 intent 无法派生订单详情可信上下文、动作顺序与业务要求不一致、成功证据归属不足、缺真实生产验收状态、缺提交点故障矩阵与并发夹具、生产冒烟只覆盖 healthz、缺唯一闲鱼 E2E 命令和复审闭环定义。

## 主流程处理

- 发布改为版本目录、预加载校验、镜像与快照成对切换及失败回滚。
- 定义服务器受控飞书认证卷、刷新持久化和脱敏身份/Base 冒烟。
- 执行顺序按业务要求改为订单发货确认后发送卡密消息。
- 增加订单页重新绑定、固定动作 intent 和同订单前后状态证据。
- 以飞书 `status + note` 定义 `shipping-attempted`、`shipped-confirmed`、`delivery-attempted`、`delivery-confirmed` 与 `manual` 跨重启语义。
- 增加消息归属、并发、提交点崩溃、迟到回执和 secret canary 场景。
- 将交付区分为 `engineering-verified`、`deployed-disabled`、`production-accepted`。
- 明确阶段命令门、独立 reviewer 绑定 commit、修复 commit 与复核门。

## 复核

修复 commit 产生后由两位原 reviewer 复核；P0/P1 清零前不得进入 Phase 1。
