# Phase 1 执行与独立复审记录

## 实施范围

- 产品展示名切换为 Zen Commerce Agent，扩展版本升至 `0.3.0`。
- `assets` 成为只安装 `xianyu-seller@0.6.0` 的生产 registry `1.0.0`。
- 生产闲鱼 pack 直接进入功能评测；非闲鱼 URL 只装配稳定基座。
- 飞书操作规则、受控认证卷、固定 `lark-cli@1.0.72` 和非 root 运行边界收口到当前仓库。
- 部署改为版本快照预校验、镜像/快照成对切换与失败成对回滚。
- 新增唯一 Phase 1 验证命令 `pnpm verify:phase1`。

## 主流程验证

- `pnpm verify:phase1`：通过。
  - dependency lint：通过。
  - 全仓 build：通过。
  - 串行单测：9 个 workspace、501 项通过。
  - AI Harness：49 个场景 × 3 跑全部通过。
  - 闲鱼生产 pack：18 个场景 × 3 跑，54/54。
  - 审计完整性：384 条事件通过 schema 与脱敏检查。
  - 报告：`evals/runs/2026-07-22-commerce-phase1.md`。
- `python3 .../skill-creator/scripts/quick_validate.py .agents/skills/release`：通过。
- `bash -n release/deploy-server.sh release/build-extension.sh`：通过。
- linux/amd64 Docker 镜像：构建通过。
  - 首轮发现 `node:22-slim` 缺 curl，官方 lark-cli postinstall 无法下载二进制；安装最小 `ca-certificates + curl` 后修复。
  - 容器内非 root `node` 用户执行 `lark-cli version 1.0.72`：通过。
  - 容器挂载 `assets` 后解析为 `1.0.0 / xianyu-seller / xianyu-orders`：通过。

首次完整验证在受限沙箱监听 mock 端口时得到 `EPERM`；相同命令在获准的非沙箱环境重跑通过，没有跳过测试。

## 独立复审

Phase commit 产生后，由架构 reviewer 与测试 reviewer 绑定 commit hash 审查；P0/P1 清零前不进入 Phase 2。
