# Phase 4 发布、回滚与生产验收记录

## 发布结果

- 最终部署代码提交：`b1b990c`。
- 服务端镜像：`zen-agent-server:b1b990c`（linux/amd64）。
- 生产快照：`/root/zen-agent/snapshots/1.1.0`。
- 活动 release：`/root/zen-agent/releases/b1b990c-20260722T053731Z-71567`。
- 公网入口：`https://agent.flash-api.com`。
- Chrome 扩展：`release/artifacts/zen-commerce-agent-extension-0.3.0.zip`，缺省服务端地址已烤入公网入口；SHA256 为 `30ca2c1395585555b5465dd991b88bd010388972e838db3850523a986a7c057b`。

## 验证结果

- 发布前 `pnpm verify:phase3` 全绿：517 项全仓测试、49 个 Harness 场景 × 3、384 条审计、闲鱼与 Side Panel Chromium E2E 均通过。
- 最终 zip：结构校验与 `unzip -t` 通过；Chromium 从 zip 解包目录加载真实 MV3 extension，Side Panel 冒烟通过。
- 远端激活：单副本、活动镜像、快照只读挂载、`/data/za` 写读删、`lark-cli --version`、本机 healthz 全部通过。实际容器挂载为 `/root/zen-agent/snapshots/1.1.0 → /app/snapshot`（`rw=false`），不是 release 内的快照链接。
- 公网 healthz：`{"ok":true}`。
- `/demo-token`：关闭。
- 回滚演练：旧 `zen-agent-server:7843bd8` + 实际旧快照成功激活并通过本机/公网 health、单副本、镜像/快照和数据卷验证；随后成功恢复 `04d3c02`，最终再发布 `b1b990c`，每次公网 health 均返回 `{"ok":true}`。

### 最小可复核证据

- 最终本地镜像 ID：`sha256:023130bcd86357667dc791e449498b3dbec2f053eb87c7070d20d24f472f6454`；远端载入镜像 ID：`sha256:dca8d3e44c7b2fdc9544acdef5a414e2d098460f97ab8cf58826eb372d78f254`。两端均核验为 `linux/amd64`，并以精确 tag `zen-agent-server:b1b990c` 激活。
- 最终 `current-release`：`/root/zen-agent/releases/b1b990c-20260722T053731Z-71567`；运行容器：`zen-agent-zen-agent-1|zen-agent-server:b1b990c|healthy`；副本数：`1`。
- 最终快照挂载：`/root/zen-agent/snapshots/1.1.0 → /app/snapshot`，`rw=false`；本机 `http://127.0.0.1:9010/healthz` 与公网 health 均为 `{"ok":true}`。
- 真实回滚路径：`bootstrap-2e70ddb-20260722T050527Z-49993`（旧 `7843bd8`）→ `04d3c02-20260722T051649Z-55552`；最终发布路径：`04d3c02-20260722T051649Z-55552` → `b1b990c-20260722T053731Z-71567`。
- 发布脚本的 Linux 行为测试覆盖 health、replica、lark、whoami、首次激活、直接 legacy 目标、legacy CAS 登记，以及 `current-release` 的 link-create/link-move 两类失败回滚。

## 发布中发现并修复的问题

1. legacy 基线登记与激活脚本在单引号 Go template 中错误转义双引号，Docker 拒绝解析挂载模板；`2e70ddb` 修复两处模板并增加静态契约，Linux 回滚行为测试通过。
2. 首次旧镜像早于外置 `system-prompt.md` 和 `lark-cli` 契约，健康旧版被新验证器误拒；`04d3c02` 为首次登记 descriptor 增加明确 `ZA_RELEASE_LEGACY=1`，只对该 descriptor 放宽后来新增的两项，继续强制健康、单副本、镜像、快照与数据卷。Linux 行为测试新增直接 legacy 目标场景并通过。
3. 第一次真实旧版演练因 CLI 新契约失败，链接未切换但容器已短暂运行旧版；主流程立即恢复 `2e70ddb` 并验证公网健康。兼容修复发布后，第二次旧版演练完整通过并恢复 `04d3c02`。
4. 独立架构审查发现最终 `current-release` 软链接创建或替换失败位于回滚事务之外；`b1b990c` 将两步切换纳入激活状态机，任一步失败都会恢复上一完整 release，并为两种失败分别增加 Linux 行为测试。

## 生产启用边界

当前状态为 `deployed-disabled`，不是 `production-accepted`：

- 生产飞书 `general` profile：未授权。
- `ZA_FEISHU_CARD_BASE_TOKEN` / `ZA_FEISHU_CARD_TABLE_ID` / `ZA_FULFILLMENT_GUIDE_URL`：未同时启用。
- `ZA_FULFILLMENT_PRODUCT_KEYS_JSON`：未配置。
- `ZA_FULFILLMENT_POLICIES_JSON`：disabled；shipment 与 delivery 均无生产预批准策略。
- Chrome 自动扫描默认关闭；没有触发任何真实闲鱼订单副作用。

已在用户现有 Chrome 登录态确认可访问 `https://seller.goofish.com/?site=COMMONPRO#/seller-trade/order-manage`，页面为“订单管理 - 闲鱼卖家工作台”，当时待发货为 0。最终 zip 已在隔离 Chromium 中通过加载和 Side Panel 冒烟，但尚未安装到该登录态 Chrome；浏览器安全策略禁止自动打开 `chrome://extensions`，而扩展安装又属于持久化操作，因此没有绕过限制。生产插件与生产服务的登录态 pack/page 只读冒烟仍待 Terry 明确安装扩展后完成；当前只能称为“服务端已部署、履约禁用”，不能关闭 `deployed-disabled` 验收。

启用前必须由 Terry 在服务器受控卷完成飞书用户授权，并在服务器 `.env` 填入真实业务坐标、商品映射及两条独立有界策略；值不得经开发机或仓库中转。重建容器后需重新跑 whoami、Base schema/只读查询、单笔低价值订单验收，才可改为 `production-accepted`。

## 独立复审

首轮独立审查绑定 `04d3c02`：

- 架构 reviewer：`P0=0 / P1=2 / P2=0`。其中发布指针切换失败不回滚已由 `b1b990c` 修复；生产登录态插件只读冒烟仍待用户安装扩展。
- 测试 reviewer：`P0=0 / P1=1 / P2=1`。P2 所需 zip 哈希、镜像 ID、切换前后 release、快照路径已补入本记录；P1 同为生产登录态插件冒烟。

原 reviewer 已复核证据提交 `d248b56`（部署代码 `b1b990c`）：

- 架构复核：指针切换事务和 legacy 最小兼容边界通过，原代码 P1 关闭；最终 `P0=0 / P1=1 / P2=0`。
- 测试/发布复核：两种指针失败回滚行为测试通过，原证据 P2 关闭；最终 `P0=0 / P1=1 / P2=0`。

两方唯一开放 P1 均为生产登录态最终扩展冒烟。该项在用户完成持久化安装前保持开放，不以受控 E2E 或本地 Side Panel 冒烟替代。
