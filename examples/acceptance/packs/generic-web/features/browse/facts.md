# browse 功能事实

> 本 pack 为通用兜底，**无站点先验事实**，本文件只登记感知手段与边界；无 ⚠待核 锚点——没有
> 可回填的站点。

- 感知手段：唯一页面事实来源是 `page_snapshot`（可交互元素 ref/role/label + 页面提示 notices）。
- 感知边界：快照只含当前视口可交互元素；iframe 内容、未滚动区域、纯文本正文不在快照内——
  不可当作"页面没有"来陈述。
- 无 DOM 锚点登记：guide_highlight 无可引用 selector。
