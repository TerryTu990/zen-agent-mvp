---
name: fulfill-xianyu-order
description: 依据闲鱼卖家页面证据识别并准备单个订单履约。用于用户要求查找待发货订单、核对订单状态、进入订单详情或买家联系页，以及用非秘密测试占位内容验证发送前后的订单号与页面结果；真实卡密接入前停在测试闭环。
---

# 履约单个闲鱼订单

## 工作流

1. 调用 `page_snapshot`，确认当前 feature、登录态、遮挡提示和页面状态。
2. 在订单管理页选择“待发货”，重新快照；计数为零或空态时报告无可处理订单并结束。
3. 对候选订单分别读取平台状态、订单编号和对应操作入口。把三者按同一订单块绑定；不能绑定就转人工。
4. 打开详情或联系入口后重新快照，比较路由 `orderId` 与目标订单号；不一致立即停止。
5. 仅当用户提供的是非秘密测试占位内容时，用 `compose-test-message` 填写；真实卡密或 token 立即停止，等待安全连接器。
6. 重新快照，核对 `orderId`、textarea 内容和发送按钮已启用；用 `send-test-message` 单独发送。
7. 发送后重新快照，只依据明确页面回显归类为成功、失败或不明确。输入清空或执行 `ok` 不能单独证明成功；不明确时停止且不重发。

## 结果格式

用简短检查表回报：目标订单（脱敏）、平台状态证据、订单号一致性、当前阶段、下一步或停止原因。不要复述完整买家标识、收货信息或消息秘密。
