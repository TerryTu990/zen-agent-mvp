---
name: fulfill-xianyu-order
description: 依据闲鱼卖家页面证据识别并完成单个订单履约。用于查找待发货订单、核对订单状态、进入买家联系页，并通过服务端零参数准备器安全领取卡密、发送固定通知及复核回执；订单发货状态更新暂不执行。
---

# 履约单个闲鱼订单

## 工作流

1. 调用 `page_snapshot`，确认当前 feature、登录态、遮挡提示和页面状态。
2. 在订单管理页选择“待发货”，重新快照；计数为零或空态时报告无可处理订单并结束。
3. 对候选订单分别读取平台状态、订单编号和对应操作入口。把三者按同一订单块绑定；不能绑定就转人工。
4. 打开详情或联系入口后重新快照，比较路由 `orderId` 与目标订单号；不一致立即停止。
5. 真实履约先调用 `page_snapshot`，确认当前聊天 URL、唯一 textarea、唯一“发 送”按钮及 `message-receipts`；再调用零参数 `prepare_xianyu_fulfillment`。工具缺失或失败立即暂停，绝不索要、读取或复述卡密。
6. 准备成功只取得 opaque `intentId`；原样调用 `xianyu-fulfillment.execute-intent`，不得添加订单号、商品号、正文或 DOM 步骤。服务端会在浏览器副作用前写入持久化 attempt 闩锁。
7. 发送后由网关强制重新快照。仅当回执数量恰好增加 1 且最新状态为“未读/已读”时判为成功并回填 `sent`；超时、换页、结果不明或飞书回填失败均暂停且不重发。
8. 只有非秘密测试占位内容才可使用 `compose-test-message` / `send-test-message`；真实履约不得降级到测试工具。当前仍不点击订单“发货”等状态动作。

## 结果格式

用简短检查表回报：目标订单（脱敏）、平台状态证据、订单号一致性、当前阶段、下一步或停止原因。不要复述完整买家标识、收货信息或消息秘密。
